# フェイストラッキングを使う方へ

フェイストラッキングを使う場合は、[自動修正（ベイクモード）](/guide/bake)がおすすめです。

表情改変によって崩れた、フェイストラッキング（FT）用の目閉じBlendShapeも修正できます。<br>
表情改変に対応しているFTアドオンも、対応していないアドオンも、同じ操作でご利用いただけます。<br>
FTアドオンのアニメーションや設定は変更しません。

※ FT用BlendShapeの修正はコンプリート版の機能です。無料版では、この設定は表示されません。

## 修正する手順

1. 表情改変を済ませ、FTアドオンを導入したアバターを用意する
2. ヒエラルキーのアバターを右クリックし、［Avatar Blink Fix］→［自動修正（ベイクモード）］を選ぶ
3. 追加されたオブジェクトのインスペクターで、FT用BlendShapeが［修正に含める］になっていることを確認する
4. アップロード後、ゲーム内で目の閉じ方を確認する

基本的には、FT用の目閉じBlendShapeも自動で修正されます。<br>
ただし、改変した表情やFTアドオンとの相性、BlendShape名などによっては、完全には修正できない場合があります。

［自動修正（推奨）］で修正済みの場合は、［ベイクモードに切り替える］を押してください。

## 設定する場所

Avatar Blink Fixのオブジェクトを選び、インスペクターの［フェイストラッキングのBlendShape］を確認してください。

- **［修正に含める］**：FT用の目閉じBlendShapeも修正します
- **［含めない］**：FT用の目閉じBlendShapeは修正しません

［詳細設定］にある［フェイストラッキング用BlendShapeも修正する］のチェックでも、同じ設定を変更できます。

::: details 設定が表示されない場合
顔のメッシュに`EyeClosedLeft`や`EyeClosedRight`があるか、プリセットにFT用BlendShapeが登録されているかを確認しています。<br>
対象が見つからない場合は、この設定は表示されません。

3.0.10以降は、`Eye_Left_Blink`／`Eye_Right_Blink`も検出対象です。
:::

::: details 片目を閉じると、反対の目の改変が戻る場合
通常は設定を変える必要はありません。<br>
この症状が出る場合は、3.0.10以降の［片目の補正範囲］で、問題のBlendShapeだけ補正する側を指定できます。

1. アバターに追加されたAvatar Blink Fixのオブジェクトを選ぶ
2. インスペクターの［詳細設定］→［片目の補正範囲（通常は設定不要）］を開く
3. ［対象を追加］から、問題が出る修正対象のBlendShapeを選ぶ
4. ［自動］から、補正したい［左］／［右］／［両側］へ変更する

左右は、アバター本人から見た左右です。<br>
たとえば右目用の`Eye_Right_Blink`で反対の目の改変が戻る場合は、［右］を試してください。

元に戻すときは、［自動］を選ぶか、その項目の［×］を押してください。<br>
そのBlendShapeだけ、自動判定に戻ります。

この設定で変わるのは、ABFが補正する範囲です。元のFTの動きや、FTアドオンのアニメーション・設定、FBXは変更しません。

メッシュ入れ替え式では、ツールの［詳細設定］から同じ項目を開きます。先に修正対象の一覧で、対象のFT用BlendShapeを選んでください。

微調整ウィンドウを開いている間は変更できません。編集を確定して閉じてから設定してください。
:::

## 表情改変をやり直したとき

プレビューを終了してから改変し、［表情を再読み込み］を押してください。<br>
FT用BlendShapeも、変更後の表情に合わせて修正し直します。

## うまく修正できないときは

［修正に含める］にしても症状が残る場合は、FTアドオン側の設定もご確認ください。

- [FTアドオンを入れると、笑顔のときに目が埋まる](/faq#ft-addon)
- [Webカメラのまばたき検知で目が崩れる](/faq#webcam)

解決しない場合は、[お問い合わせ](/faq#contact)ください。
