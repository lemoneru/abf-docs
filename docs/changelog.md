# 更新履歴

VCC / ALCOM で導入している場合、新しいバージョンは「更新」ボタンを押すだけ。有料版のファイルを入れ直す必要もありません。

## 3.0.7（2026-08-28）

- **MMD用のBlendShapeで表情改変していると、まばたきの修正が効かない**ことがある問題を修正しました
- **顔のメッシュを自動で見つけられるアバターが増えました。** これまでは `Body` という名前しか探していなかったので、`Face` などの名前のアバターでは自動修正が選べませんでした
- 顔のメッシュを手で選び直したときに、そのまま修正まで進むようになりました
- 同梱プリセットを4件修正しました（MUMUS 1.0・Wolferia・Marycia 1.5.1・Carroll）

## 3.0.6（2026-08-28）

- 3.0のベータ版から更新したときに、**ベータ版の古いプリセットが新しいものより優先されてしまう**問題を修正しました（ご自身で追加したプリセットは今までどおり消しません）
- プレイモードから編集画面へ戻ったときに、プレビューの見た目が崩れたままになる問題を修正しました

## 3.0.5（2026-08-28）

- **NDMF を VPM の依存関係として宣言しました。** VCC / ALCOM で追加すると NDMF も一緒に入ります
- **ベイク対象の一覧を色分けしました**（シアン＝目のまわり／オレンジ＝目以外）。外していいものが見て分かります
- 前のバージョンが残っているときの案内を「**移行**」の言い方に作り直しました（確認は1回だけ。移したファイルの一覧は Console に出ます）
- インスペクタに **[使い方（Web）]** ボタンを追加しました。VCC / ALCOM の「Documentation」リンクもこのドキュメントに変わりました
- プロジェクトを開き直した直後に、修正が反映されていない見た目になる問題を修正しました（アップロード結果には影響していませんでした）
- 手動修正を使いやすくしました（表情改変に使った BlendShape は修正対象に選べないように／対象が0件でもプレビューを押せるように／最初から編集できる状態で開くように）

## 3.0.4（2026-08-27）

- フェイストラッキング対応を拡充: `EyeClosed`・`EyeClosedJoyful` 系の BlendShape も修正対象として検出するようになりました

## 3.0.3（2026-08-27）

- 画面の表示を全体的に整理し、読まなくても操作できる見た目にしました（説明は各見出し横の「?」に格納）
- ボタン名の見直し: 「自動セットアップ」→「**表情を再読み込み**」（あとから表情改変を変えたときに押すボタンです）
- Avatar Mouth Fix を導入しているプロジェクトで、併用手順の案内を表示するようにしました。あわせて、対応外の組み合わせで修正の記録が壊れないよう保護を追加しました

## 3.0.2（2026-08-26）

- 同梱プリセットを増やしました（プリセット133本・対応アバター100体以上）
- テスターの皆さまのフィードバックを反映し、表示や操作を改善しました（項目名の見直し・右クリックメニューの並び順・案内文言の改善など）
- ベイクモードで、販売元の初期値のままの BlendShape を自動選択から除外するようにしました（付属アニメーション再生時に顔が崩れる問題の防止）
- ベイクモードでフェイストラッキング用 BlendShape を検出した場合、既定で修正に含めるようにしました（トグルで外せます）
- 解除キーの導入に成功したとき、「コンプリート版になりました」の案内を表示するようにしました

## 3.0.1（2026-08-25）

- 配布パッケージの整備と、VCC / ALCOM 導入環境向けの案内表示の調整を行いました

## 3.0.0 — 3.0 の大きな変更

### 大きな変更

- **非破壊化**: アバターを右クリック →「自動修正（推奨）」だけで完了する方式になりました（NDMF 対応）。メッシュを書き換えず、アップロード時に自動で修正が適用されます。追加されたオブジェクトを削除すればいつでも元通りです
- 今までのウィンドウ（2.x と同じ方式）も「**メッシュ入れ替え式**」としてそのまま残っています。NDMF を入れていない環境や、他ツールと相性が出た場合の回避経路としてご利用いただけます
- 2.x で修正済みのアバターはそのまま使い続けられます。3.0 を上書きインポートしても壊れません（右クリック修正しようとした場合は検知して案内を出します）

### 新機能

- **競合の自動チェック**: 表情アニメーションが修正対象の BlendShape を操作している場合に検知して案内し、ベイクモードへの切り替えを提案します
- **ベイクモードも非破壊に**: 改変した表情を基礎メッシュへ焼き込む処理もビルド時に自動適用されます
- BlendShape の**限界突破**（100 超・マイナス値）に対応しました
- **フェイストラッキング (FT)** アバターの目閉じ BlendShape を検出し、修正対象に含めるか選べるようになりました
- **微調整設定の書き出し / 読み込み**: 設定を JSON に保存し、Blender で作り直したアバターにも読み込めます
- **Animation 合成**で、PhysBone やオブジェクトの ON/OFF など BlendShape 以外のキーも消えずに引き継がれるようになりました
- **保存データの管理**: ツールが保存した記録（バックアップ等）を一覧で確認・削除できるようになりました
- **サポート情報のコピー**: 問い合わせ時に貼り付けるだけで状況が伝わる診断コードを出力できます
- **VCC / ALCOM 対応**（VPM 配布）: 更新のたびに入れ直す必要がなくなりました
- 対応言語: 日本語 / English / 한국어 / 中文

### トライアル版

- トライアル版を一新し、製品版と同じ 3.0 の操作感（右クリック導線・プレビュー・復元）で試せるようになりました。修正できるのは自動選択された1つの BlendShape のみです
- トライアル版の上から製品版（解除キー）を導入するだけで、残骸なくそのまま製品版になります

### 修正・改善

- エラーメッセージを「原因 + 次にすること」が分かる形へ全面的に見直しました
- プリセット未導入時は、エラーではなく案内を表示するようにしました
- 危険な BlendShape（顔全体が変わるもの等）の警告を分かりやすくしました

### 既知の注意点

- NDMF のプレビューが停止すると修正が見えなくなることがあります。Unity を再起動すると回復します
- シーン上のメッシュが変わって見えないのは正常です（修正はアップロード時に適用されます）
